# Tracked in git as a safe fallback so the project builds without a real key
# (e.g. a fresh clone or CI). Maps tiles simply won't render with this value.
# Real key goes in local.properties (gitignored) as MAPS_API_KEY=...
MAPS_API_KEY=DEFAULT_API_KEY_REPLACE_ME
